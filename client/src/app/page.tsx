import Dashboard from "@/app/dashboard/page" //@ alias refers to the source directory
                                              //prevents us from having to use a relative path
export default function Home() {
  return (
    <Dashboard />
  );
}
